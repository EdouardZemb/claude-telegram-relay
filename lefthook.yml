pre-commit:
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: bunx biome check --diagnostic-level=error --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
    typecheck:
      run: bunx tsc --noEmit
